google-api-python-client==2.58.0

# NOTE: youtube-transcript-api is now pinned in django_requirements.txt (>=0.6.0) to avoid resolver conflicts
# youtube-transcript-api==0.5.0  # legacy pin (conflicts with >=0.6.0); kept as comment for reference

langchain
openai
tqdm==4.64.1
transformers
python-dotenv==1.0.1
langchain_community
langchain_openai
whisper
gunicorn==21.2.0
django==4.2.7
# djangorestframework版本统一在django_requirements.txt中管理，避免冲突

# Admin UI enhancements (added to satisfy INSTALLED_APPS)
django-admin-interface>=0.29.0
django-colorfield>=0.11.0
djangorestframework-simplejwt==5.3.0
# Third-party Django apps required by INSTALLED_APPS (synchronized subset of django_requirements.txt)
django-cors-headers==4.3.1
django-filter==23.4
django-extensions==3.2.3
django-import-export==3.3.3
django-guardian==2.4.0
django-redis==5.4.0
django-celery-beat==2.5.0
celery[redis]==5.3.4
channels==4.0.0
channels-redis==4.2.0
psycopg2-binary==2.9.9
dj-database-url==2.1.0
drf-spectacular==0.26.5
drf-spectacular-sidecar==2023.10.1
whitenoise==6.7.0

# Video downloaders: prefer yt-dlp; youtube_dl retained as legacy reference
# youtube_dl               # legacy; module is yt_dlp at runtime
yt-dlp
pydub
# 音频处理增强支持 (pyaudio已从Docker构建中移除，在本地开发中可选)
soundfile
librosa
scipy
numpy
# API限流和异步支持
aiolimiter
# 测试框架
pytest
pytest-django
# sqlite3-rich 
pytest-asyncio
sqlalchemy
# 视频下载
pytube
browser_cookie3
pyyaml>=6.0

# Stdlib / system-provided entries kept as comments to reduce pollution
# asyncio                 # stdlib
# dataclasses             # stdlib (3.7+)
typing-extensions
# ffmpeg                  # system binary (installed via apt in Docker)
# psutil                  # optional legacy comment
psutil>=5.9,<6.0

# Optional (CLI): add when building fresh envs
# aiohttp>=3.9           # required for cli.py HTTP pooling